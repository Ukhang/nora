---
title: Getting Started
description: Noreo is a compact, smooth, and minimal loader component for React.
---

### Installation
Simply pnpm/npm/yarn install the package.

<Tabs defaultValue="pnpm" className="pt-5 pb-1">
  <TabsList>
    <TabsTrigger value="pnpm">pnpm</TabsTrigger>
    <TabsTrigger value="npm">npm</TabsTrigger>
    <TabsTrigger value="yarn">yarn</TabsTrigger>
  </TabsList>
  <TabsContent value="pnpm">
    ```bash:bash
    pnpm i noreo
    ``` 
  </TabsContent>
  <TabsContent value="npm">
    ```bash:bash
    npm i noreo
    ```  
  </TabsContent>
  <TabsContent value="yarn">
    ```bash:bash
    yarn add noreo
    ```  
  </TabsContent>
</Tabs>

### Create a Loader component
It can be placed anywhere in your app.

```jsx:MyComponent.jsx
use client;

import { Noreo } from 'noreo';

function MyComponent() {
  return (
      <Noreo variant="spinner" />
  );
}
```